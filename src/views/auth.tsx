import React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'native-base';

const AuthView = () => {
  return (
    <SafeAreaView>
      <Text color="primary.500">Auth View</Text>
    </SafeAreaView>
  );
};

export default AuthView;
