import React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'native-base';

const HomeView = () => {
  return (
    <SafeAreaView>
      <Text color="primary.500">Home View</Text>
    </SafeAreaView>
  );
};

export default HomeView;
